- [x] Scan `src/assets/images/gallery` and confirm image filenames

- [x] Update `src/components/Gallery/Gallery.jsx` to import all images from the gallery folder and render them

- [x] Ensure `Gallery.jsx` uses a safe, build-compatible approach (no runtime fs)

- [x] Run `npm test` and/or `npm run build` to confirm no missing-import errors


